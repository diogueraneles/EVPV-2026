# Copie para .env (o .env NAO deve ir para o git).

# ---- Rodar a ingestão/scripts apontando para o Postgres ----

# Supabase (uso LOCAL — direct connection; requer IPv6 na sua rede):
# DATABASE_URL=postgresql://postgres:SUA_SENHA@db.rvcfbklnwglmhoxfexvj.supabase.co:5432/postgres?sslmode=require

# Supabase para GITHUB ACTIONS / CODESPACES: use a string do POOLER (IPv4),
# copiada do painel do Supabase.
# NAO coloque a senha real aqui neste arquivo de exemplo — só no .env / Secret.

# ---- API (opcional) ----
# API_KEYS=chave1,chave2   # se definido, a API exige o header X-API-Key
